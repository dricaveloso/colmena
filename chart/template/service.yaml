apiVersion: v1
kind: Service
metadata:
  name: {{ include "k8deploy.fullname" . }}
  labels:
    {{- include "k8deploy.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "k8deploy.selectorLabels" . | nindent 4 }}
