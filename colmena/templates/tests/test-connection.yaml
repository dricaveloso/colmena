apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "colmena.fullname" . }}-test-connection"
  labels:
    {{- include "colmena.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "colmena.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
